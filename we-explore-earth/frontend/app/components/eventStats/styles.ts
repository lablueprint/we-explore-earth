import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  header: {
    fontSize: 22,
    fontWeight: "600",
    marginBottom: 8,
    textAlign: "center",
  },
  subheading: {
    color: "#000000ff",
    marginBottom: 12,
    fontSize: 18,
    textAlign: "center",
  },
  sectionTitle: {
    marginTop: 16,
    fontSize: 16,
    fontWeight: "500",
  },
});